<%= form_for [@building,@unit] do |f| %>
<%= f.error_messages %>
  <p>
  	<%= f.label :unit_num, "Unit Number" %>	
  	<%= f.text_field :unit_num %>
  </p>
  <p>
  	<%= f.label :rent %>	
  	<%= f.text_field :rent %>
  </p>
  <%= f.submit %>
<% end %>